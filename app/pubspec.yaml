name: cogniread_app
description: CogniRead MVP skeleton (Flutter)
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7

  file_picker: ^8.1.2
  path_provider: ^2.1.4
  uuid: ^4.5.1
  epub_view: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
